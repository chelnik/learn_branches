/*var colors = ['red', 'green', 'blue', 'orange', 'yellow'];

function getRandomIndex(arr){
    let randomIndex = Math.random() * arr.length;
    return arr(randomIndex)
}
varColor = getRandomIndex(colors);
console.log(varColor);*/
on